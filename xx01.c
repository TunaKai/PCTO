#include <stdio.h>

int main()
{
    char f = '%';
    float g = 1.1 ;
    double h = 2.22;
    printf("%c %.1f %.2lf\n", f,g,h);
    return(0);
}

 printf("Your Answer: %d\n", AA);
        }else if("myChar" == " - ")
            {      int BB = n1 - n2;
                    printf("Your Answer Is: %d\n", BB);
                        }else if("myChar" == " * ")
                            {   int CC = n1 * n2;
                                printf("Answer: %d\n", CC);
                                }else("myChar" == " / ");\
                                {   float DD = n1 / n2;
                                    printf("Answer: %.1f\n", DD);
