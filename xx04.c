#include <stdio.h>

int main ()
{
    int n1;
    int n2;
    char character;
    int answer;
    int answer2;    
    printf("Pick A Number:");
    scanf(" %d", &n1);
    printf("Pick A Character:");
    scanf(" %c", &character);
    printf("Pick A Number:");
    scanf(" %d", &n2);
    if ("character == +")
    {
        answer = n1 + n2;
        printf(" %d", answer);
        case ("character == -")
        answer2 = n1 - n2;
        printf(" %d", answer2);
    

}